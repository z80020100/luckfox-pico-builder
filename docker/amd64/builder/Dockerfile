# check=skip=FromPlatformFlagConstDisallowed
# Luckfox amd64 build environment built on the official Ubuntu 22.04 SDK image.
# Pinned to linux/amd64 because the bundled toolchain ships x86-64 binaries only.
FROM --platform=linux/amd64 luckfoxtech/luckfox_pico:1.0

WORKDIR /workspace

CMD ["/bin/bash"]
